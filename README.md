# Node.js Binding Generator

## Usage
1. Run: `mkdir output && cd output && npm init --yes && npm i -S uniffi-bindgen-react-native ffi-rs && npm i -D @types/node`
2. Run `cargo run -- <lib_path>` in the root of the project
3. The library output is in _output/livekit_uniffi_node.ts_
